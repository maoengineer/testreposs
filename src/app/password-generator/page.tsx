import type { Metadata } from "next";
import ToolLayout from "@/components/tools/ToolLayout";
import PasswordGeneratorClient from "@/components/tools/PasswordGeneratorClient";
export const metadata: Metadata = { title: "Password Generator — Secure Random Passwords | iUseTools", description: "Generate strong, random passwords using cryptographic randomness. Customize length, character sets, and generate multiple at once." };
const tool = { name: "Password Generator", description: "Generate cryptographically secure random passwords", icon: "🔐", color: "rose", bgGradient: "from-rose-500 to-pink-600", inputFormats: ["Settings"], outputFormat: "Password", howTo: ["Set desired password length","Choose character types to include","Generate one or multiple passwords","Copy and save securely"], features: ["Uses browser crypto API — truly random","Control length from 4 to 64 chars","Toggle uppercase, lowercase, numbers, symbols","Exclude similar-looking characters","Password strength indicator","Generate up to 10 at once"], faqs: [{ q:"Is this cryptographically secure?",a:"Yes, passwords are generated using the Web Crypto API (crypto.getRandomValues), which is cryptographically secure." },{ q:"Are passwords stored anywhere?",a:"No. Passwords are generated in your browser and never sent to any server." }] };
export default function Page() { return <ToolLayout tool={tool}><PasswordGeneratorClient /></ToolLayout>; }
